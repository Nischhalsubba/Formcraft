name: Quality

on:
  push:
    branches: [main]
    paths:
      - 'app/**'
      - '.github/workflows/quality.yml'
      - 'netlify.toml'
  pull_request:
    branches: [main]
    paths:
      - 'app/**'
      - '.github/workflows/quality.yml'
      - 'netlify.toml'

permissions:
  contents: read

concurrency:
  group: formcraft-quality-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    defaults:
      run:
        working-directory: app
    env:
      SUPABASE_URL: https://formcraft-test.supabase.co
      SUPABASE_PUBLISHABLE_KEY: sb_publishable_formcraft_browser_test_key_1234567890
    steps:
      - name: Check out repository
        uses: actions/checkout@v5
      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
      - name: Build and run complete verification suite
        run: npm run build
