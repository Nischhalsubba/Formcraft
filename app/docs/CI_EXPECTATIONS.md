# CI expectations

A green release requires both repository workflows:

- Interaction audit
- Dynamic UI validation

The dynamic workflow must complete JavaScript syntax, static contracts, model tests and authenticated Chromium regression checks. A Netlify-ready build alone is not evidence that the application works.
