# Ready-to-test definition

A capability is ready to test only after its implementation exists, persistence paths are connected, relevant syntax and model tests pass, and the authenticated browser workflow reaches it without console or page errors.
